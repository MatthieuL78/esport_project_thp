<div class="next">
  <h2>up next</h2>
  <div class="section">


      <% @events.each do |event| %>
      <div class="article">
          <div class="article-image">
          <%= image_tag("#{event.image}") %>
          </div>
          <div class="article-info">
            <h3><%= event.name %></h3>
            <p><%= event.place %></p>
            <p><%= event.name %></p>
            <p><%= event.name %></p>
            <p><%= event.name %></p>
            <p><%= event.name %></p>
          </div>
        </div>
      <% end %>



<!-- tournanament archives -->
<div class="past">
  <h2>past</h2>

  <% @events.each do |event| %>
  <div class="article">
      <div class="article-image">
      <%= image_tag("#{event.image}") %>
      </div>
      <div class="article-info">
        <h3><%= event.name %></h3>
        <p><%= event.place %></p>
        <p><%= event.name %></p>
        <p><%= event.name %></p>
        <p><%= event.name %></p>
        <p><%= event.name %></p>
      </div>
    </div>
  <% end %>


</div>
